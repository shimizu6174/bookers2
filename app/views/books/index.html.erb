<div class="container">
	<div class="row">
		<div class="col-lg-3"> 
			<%= render 'users/info' %>
			<%= render 'books/new' %>
		</div>

		<div class="col-lg-9">
			<h2>Books</h2>
			<table class="table">
				<thead>
					<th></th>
					<th>Title</th>
					<th>Opinion</th>
				</thead>

				<body>
					<% @books.each do |book| %>
					<tr>
						<td><%= attachment_image_tag get_user(book),:profile_image, size: "50x50",fallback: "no_image.jpg"%></td>
						<td><%= link_to book.title, book_path(book.id) %></td>
						<td><%= book.body %></td>
					</tr>
					<%end%>
				</body>
			</table>
		</div> 

	</div>
</div>                
